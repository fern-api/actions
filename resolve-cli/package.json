{
  "name": "@fern-github-actions/resolve-cli",
  "version": "0.0.0",
  "private": true,
  "description": "GitHub Action: resolve the Fern CLI command to use",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@fern-github-actions/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
