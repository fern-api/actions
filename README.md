# preview dist branch

Holds published `preview` artifacts. Each commit corresponds to a release;
tags `preview@vX.Y.Z` point at specific releases.

Source code lives on `main`. **Do not edit this branch by hand** — the
[release workflow](https://github.com/fern-api/actions/actions/workflows/release.yml)
manages it.
