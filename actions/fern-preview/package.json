{
  "name": "@fern-github-actions/fern-preview",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action to publish preview SDKs and post PR comments with install links and SDK diff links",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.1",
    "@fern-github-actions/shared": "workspace:*",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.5",
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "overrides": {
    "undici": "^6.23.0"
  },
  "engines": {
    "node": ">=20"
  }
}
